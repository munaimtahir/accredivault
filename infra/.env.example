# Database Configuration
POSTGRES_DB=accredvault
POSTGRES_USER=accredvault
POSTGRES_PASSWORD=change_me_in_production

# MinIO Configuration
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin_change_me_in_production

# Backend Configuration
SECRET_KEY=django-insecure-change-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# For production, set DEBUG=False and specify proper ALLOWED_HOSTS
# ALLOWED_HOSTS=your.domain.tld,www.your.domain.tld
