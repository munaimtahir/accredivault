# Tests (minimum)

Unit:
- rule evaluator for all rule types
- status precedence with verification
- signed URL generation

API:
- import PHC CSV (118 rows) + publish pack
- upload evidence + link to control
- export control PDF smoke test

E2E (later):
- login → control → upload evidence → READY → export
