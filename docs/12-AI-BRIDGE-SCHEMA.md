# AI Bridge (Design only; Disabled)

Purpose: export a JSON packet for manual AI-agent processing and import validated JSON response.

- Export includes: pack metadata, controls, evidence metadata, expected schema
- Import: strict validation; saved as non-authoritative assistant notes/suggestions
- No auto-compliance changes without human approval
